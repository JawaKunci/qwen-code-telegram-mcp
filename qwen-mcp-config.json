{
  "mcpServers": {
    "telegram": {
      "command": "node",
      "args": [
        "[PROJECT_PATH]/dist/index.js"
      ],
      "env": {
        "TELEGRAM_BOT_TOKEN": "${TELEGRAM_BOT_TOKEN}"
      }
    }
  }
}
